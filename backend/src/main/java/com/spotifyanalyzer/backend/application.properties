# Spotify API Configuration
spotify.client.id=${SPOTIFY_CLIENT_ID:3e16468be5e749b39e9700d43b422059}
spotify.client.secret=${SPOTIFY_CLIENT_SECRET:79eeb4165358465fbaed3efb413135a7}
spotify.redirect.uri=${SPOTIFY_REDIRECT_URI:http://localhost:8080/api/spotify/callback}

# Session Configuration
server.servlet.session.timeout=1h
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.same-site=lax

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=Authorization,Content-Type
cors.allow-credentials=true
cors.max-age=3600

# Logging Configuration
logging.level.com.yourapp=INFO
logging.level.org.springframework.web=INFO

# Server Configuration
server.port=8080